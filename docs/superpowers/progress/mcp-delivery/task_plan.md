# MCP 完整交付计划

## 目标

完成已确认的 MCP 连接配置、专属工作台/新建/详情/历史、专属 API 和安全执行链路，验证后合并并推送 origin/develop。用户已于 2026-09-07 明确授权完整开发及上传 develop。

## 当前阶段

1. [完成] 保存已有运行时检查点，合并最新 develop，保留 AI / Skills / Agent 改动和数据库升级兼容。合并提交 b0ca2dd54。
2. [完成] MCP 专属任务查询/取消/附件、连接配置 HTTP 合同和服务器装配，封堵旧通用 MCP 入口；API 规格与质量审查 PASS。
3. [完成] MCP 专属 React 页面与凭证配置页面，中文、两类来源、选配置、专属跳转；规格与质量审查 PASS。
4. [完成] 受控 Git/附件归档、Agent/Python 私有运行时及可选模型执行，规格与质量复审 PASS。
5. [完成] API 三件套、跨层回归、构建及最终两阶段审查；真实无模型代码任务生成报告。新增浏览器登录验收仍待用户许可，不声称已完成该项。
6. [进行中] 提交、合并 develop、推送、核验远端 SHA；保留功能分支与工作区。

### 用户确认：模型可选（2026-09-07）

- [完成] 不选模型时执行无 LLM 的基础只读检查，不回退默认模型；真实附件→Agent→Python→成功报告已验证。
- [完成] 新建页/详情/报告明确基础检查范围、待复核线索和参考评分，保持既有浏览器接口及整数评分合同。
- [完成] 模型辅助仓库只读白名单、UTF-8 私有协议、无效模型报告失败关闭；最终运行时规格/质量复核 PASS。
- [待许可] 本轮浏览器自动登录被权限检查拦截，已询问用户是否同意使用隔离测试账号；不绕过登录验收。

## 约束

- 使用已有 codex/mcp-scan-workbench 工作区，不改主目录或其他用户工作区。
- 不改已有未跟踪的原型与历史计划；本目录单独记录此次交付。
- 页面延续已批准原型及现有控制台 token，属于 Extension；不另做风格探索。
- 保留 develop 已发布的 v10 remark/target_count 迁移，MCP 迁移排在其后并兼容已有 MCP 开发库。
- 先失败测试再实现；未通过联调/审查不得把半成品推送 develop。

## 已知基线与问题

- 本地没有 Go，使用现有 PostgreSQL Docker 测试环境；Node 可直接执行本地包脚本。
- 旧全量套件有缺文件/Chrome/外部服务/旧接口断言失败，需区分环境与本次回归。
- develop 合并冲突已解决并完成两阶段审查，保留两侧有效行为。
