# 任务进度

后续 HTTP 支持已完成：默认关闭、显式允许的 HTTP 凭据已贯通 UI/API/任务/Agent/传输；743 个前端测试通过，HTTP 实测成功，无新增 Go 基线失败。详见 [HTTP 验收](http-verification.md)，原 HTTPS-only 范围按新规格扩展。

- 已完成：隔离工作树、规格/计划审查、凭据资源、迁移 13、HTTP 隔离、任务私有认证通道、控制台、API/Swagger 同步及定向验证。
- 已完成：固定版本 Node/pnpm 的前端全量 733 测试、Lint/typecheck/build；TaskPages 全部 67 测试通过。
- 已完成：完整 Go 测试执行并与基线对比；数据库、任务服务、凭据资源、HTTP、runner、apidocs 包通过，其余失败均在基线已存在。
- 已完成：认证扫描读取随 Agent 发布的本地规则，认证主请求失败不再生成成功报告。
- 已完成：HTTPS 靶场成功认证、错误 Token 失败、反射证据脱敏；浏览器创建、编辑和删除测试凭据；清理靶场与额外测试 CA。
- 已完成：最后规则空文件/部分不可读回归和独立复核（Approved），重建并启动最终预览镜像。
- 已完成：清理本次基线、开发、数据库和靶场测试容器；预览服务健康，Agent 运行且无测试 CA；仅 127.0.0.1:8089 监听，8088/8090/4176 已空闲。

状态：实施与验收完成。代码保留于独立工作树和 codex/infra-target-credentials 分支，未提交、合并或推送；根工作区原有未跟踪内容保持不变。

## 决策
凭据私有；绑定 HTTPS 源；一个任务一个凭据，多个同源手动 URL；禁止自动发现/附件；密钥不进入任务存储；编辑使用版本锁。
