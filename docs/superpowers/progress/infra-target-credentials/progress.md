# 工作记录

2026-09-07：从 develop f5073af12 创建 codex/infra-target-credentials。完成规格、计划和独立审查。

实现私有目标凭据 CRUD、AES-GCM/AAD、If-Match 版本保护、迁移 13；任务只保存引用，下发前重验并通过具备 infra-target-auth-v1 能力的 Agent 私有通道解密。传输限制同源 HTTPS，拒绝不安全绕过、清除响应反射，并在事件存储前再次脱敏。

审查发现并修复断连残留脱敏上下文、Chromium 截图旁路、失效选择自动清空、IPv6 规范差异，以及上传目标清单后无法移除的问题。修复均有先失败后通过的回归证据；密钥页面和任务页面完整相关测试通过。

校验：完整控制台最终 50 文件/733 测试通过，Lint、typecheck 和 build 通过；包含附件恢复新增用例，TaskPages 67 测试通过。完整 Go 测试日志在外部任务产物目录 infra-target-credentials-go-tests.log；对比基线未发现本次新增失败，数据库与新增功能相关包通过。

预览已备份数据库并应用迁移 13，8089 新入口可见；浏览器验证创建、编辑留空保留/版本 1→2、替换密钥/版本 2→3、跨源提交阻断和删除。Agent 旧知识库 API 返回 401 的问题已用认证任务读取本地规则解决，知识库鉴权保持不变。

HTTPS 端到端实测完成：正确 Token 收到 103 次认证请求并生成报告；故意回显 Token 后，task/session、21 条事件、报告原始与渲染数据均无 Token。错误 Token 只收到一次 401，任务失败且无成功报告，11 条事件无 Token。临时凭据和靶场已删除，Agent 额外测试 CA 已移除；带明确验收备注的任务和报告保留供复核。

最后增量审查补齐空白/注释/零值规则与部分不可读漏洞库的拒绝处理，仅认证模式使用严格入口。13 类错误和恢复、匿名兼容、实际中英文规则加载等定向回归通过，独立复核 Approved。最终镜像 b15c50bbaff5 已运行；测试容器清理完毕，预览仅监听 127.0.0.1:8089，8088/8090/4176 空闲。实施和验收完成，保留独立工作树与分支，不自动提交、合并或推送。

用户随后确认内网均使用 HTTP。已加入默认关闭的明文 HTTP 许可，协议保存在参与 AAD 的 origin，View/runtime 派生许可，HTTP 写入必须明确确认；Agent 升级能力 v2，仍严格禁止跨协议/主机/端口传播认证。独立规格和代码复核通过，前端全量 743 测试、定向 Go 和实际 HTTP 扫描通过；全量 Go 与 develop 基线对比仍为原 14 个失败。HTTP 临时凭据和测试容器已清理，预览更新至镜像 3e93ecd142be，用户已有配置保留。详细证据见 http-verification.md。

用户进一步要求默认支持 HTTP，直接填写不报协议许可错误，不显示“允许明文 HTTP”。已移除开关与许可提示，公共 API 按 origin 默认接受 HTTP/HTTPS，旧请求布尔字段忽略；内部协议派生和同源/AAD 边界保持。前端全量 743 测试及构建、相关 Go 整包和独立复核通过。浏览器直接创建与编辑 HTTP 凭据成功，认证扫描完成并收到 103 次正确请求，脱敏检查通过。临时资源已清理，预览更新至 930eb19db90b；详情见 http-default-verification.md。此前显式许可交互记录由本轮取代。
